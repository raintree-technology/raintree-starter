import Link from "next/link";
import { RecoverableErrorBoundary } from "@/components/error-boundary";
import { Logo } from "@/components/logo";
import { features } from "@/lib/config";

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-dvh flex-col">
      <header className="border-b border-border bg-card">
        <nav aria-label="Main navigation" className="content-width flex min-h-16 flex-wrap items-center justify-between gap-4 py-3">
          <Logo />
          <div className="flex gap-5 text-sm">
            <Link href="/">Home</Link>
            {features.billing && <Link href="/pricing">Pricing</Link>}
            <Link href="/login">Sign in</Link>
          </div>
        </nav>
      </header>
      <main id="main-content" className="flex-1"><RecoverableErrorBoundary title="This page could not load." description="Try again, or return home." homeHref="/">{children}</RecoverableErrorBoundary></main>
      <footer className="content-width w-full border-t border-border py-6 text-sm text-muted-foreground">
        <Link href="/privacy">Privacy</Link>
      </footer>
    </div>
  );
}
