import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { site } from "@/lib/discovery";
import { createPageMetadata } from "@/lib/metadata";

export const metadata = createPageMetadata({
  title: site.name,
  description: "A workspace for your team and clients.",
  path: "/",
});

export default function HomePage() {
  return (
    <section className="content-width section-gap">
      <p className="text-caption">Reference workspace</p>
      <h1 className="mt-3 text-4xl font-semibold tracking-tight">{site.name}</h1>
      <p className="mt-4 max-w-xl text-body text-muted-foreground">
        Sign in to manage your projects, invite colleagues, and update your account.
      </p>
      <div className="mt-6 flex flex-wrap gap-3">
        <Button asChild><Link href="/login">Sign in</Link></Button>
        <Button asChild variant="outline"><Link href="/signup">Create account</Link></Button>
      </div>
      <Card className="mt-12 max-w-xl p-6">
        <h2 className="text-title">Your workspace starts here</h2>
        <p className="mt-2 text-sm text-muted-foreground">
          Create an account and organization to start your first project. Your dashboard
          shows projects for your active organization.
        </p>
      </Card>
    </section>
  );
}
